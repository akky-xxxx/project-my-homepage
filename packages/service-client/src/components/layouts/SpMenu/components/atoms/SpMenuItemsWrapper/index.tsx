import styled from "styled-components"
import { ZIndexes } from "../../../const/ZIndexes"

export const SpMenuItemsWrapper = styled.div`
  left: 0;
  position: fixed;
  right: 0;
  z-index: ${ZIndexes.Items};
`

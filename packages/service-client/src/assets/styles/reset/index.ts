import { css } from "styled-components"

export const reset = css`
  body {
    font-family: arial, sans-serif;
    margin: 0;
  }

  ul,
  ol {
    padding: 0;
  }

  li {
    list-style: none;
  }
`
